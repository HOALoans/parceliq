# Copy this to .env and fill in your values
# NEVER commit .env to git

# MySQL — get this from Render after creating a MySQL database
DATABASE_URL=mysql://user:password@host:3306/parceliq

# App
NODE_ENV=development
PORT=10000
